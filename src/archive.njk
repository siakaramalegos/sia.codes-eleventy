---
layout: layouts/home.njk
title: "Sia Karamalegos: Blog Archive"
description: Learn about web performance, Eleventy, and the Jamstack with writing by Sia Karamalegos, performance engineer and web developer.
tags:
  - nav
navtitle: Blog
permalink: /posts/
---

<div class="content">
  <section class="flow">
    <h1>Blog</h1>
    <p>Learn about web performance, Eleventy, and the Jamstack.</p>
    <p>See <a href="#select-posts">select posts</a>, <a href="#all-posts">all posts</a>, or <a href="#filter-by-topic">filter by topic</a></p>
  </section>

  <section class="flow" id="filter-by-topic">
    <h2>Filter by topic</h2>
    <p>Or <a href="https://www.google.com/search?q=site%3Asia.codes%2Fposts+eleventy">search with Google</a></p>
    <p style="max-width: 600px;">
      {% set tags = collections.tagList | sort %}
      {% for tag in tags %}
        {%- if tag != "posts" -%}
        {% set tagUrl %}/tags/{{ tag }}/{% endset %}
        <a href="{{ tagUrl | url }}" class="post-tag">{{ tag }} <span>({{ collections[ tag ] | length }})</span></a>
        {%- endif -%}
      {% endfor %}
    </p>
  </section>

  <section class="flow" id="select-posts">
    <h2>Select posts</h2>
    {% set postslist = collections.posts | getSelect %}
    {% include "postslist.njk" %}
  </section>

  <section class="flow" id="all-posts">
    <h2>All posts</h2>
    {% set archivelist = collections.posts %}
    {% include "archivelist.njk" %}
  </section>
</div>
